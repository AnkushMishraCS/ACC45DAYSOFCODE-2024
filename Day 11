#include <iostream>
using namespace std;

int main() {
    int T, X, Y;
    cin >> T;

    while (T--) {
        cin >> X >> Y;

        if (X >= Y) {
            cout << 0 << endl;
        } else {
            cout << (Y - X + 1) / 2 << endl;
        }
    }

    return 0;
}
